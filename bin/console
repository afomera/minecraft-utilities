#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require_relative "../minecraft/server_download"

puts "Try using something like `Minecraft::ServerDownload.jar(stable: true)` to get the latest stable server jar."

require "irb"
IRB.start(__FILE__)
